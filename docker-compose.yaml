version: '3.8'

services:
 client:
  build:
   context: ./client
  ports:
   - 3000:3000
  command: npm start
 server-nest:
  build:
   context: ./server-nest
  ports:
   - 3002:3002
  command: npm start