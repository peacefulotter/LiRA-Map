import { getRide } from '../../queries/rides';
import { useMetasCtx } from '../../context/MetasContext';
import { RideMeta, TripsOptions } from '../../models/models';

export const filteredData = (
  allRides: RideMeta[],
  selectedRides: RideMeta[],
  selectedOptions: TripsOptions[],
) => {};
