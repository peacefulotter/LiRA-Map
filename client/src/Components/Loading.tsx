import React, { FC } from 'react';
import '../css/loading.css';

const Loading: FC = () => {
  return (
    <div className="loading">
      <span>Loading...</span>
    </div>
  );
};

export default Loading;
